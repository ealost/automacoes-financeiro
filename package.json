{
  "name": "automacoes-financeiro",
  "version": "0.1.0",
  "private": true,
  "description": "Painel dos projetos de automação do Financeiro Castilho (site estático + Firestore).",
  "type": "module",
  "scripts": {
    "seed": "node scripts/seed.mjs",
    "serve": "firebase serve --only hosting",
    "deploy": "firebase deploy --only hosting,firestore:rules"
  },
  "devDependencies": {
    "firebase-admin": "^12.3.0"
  }
}
